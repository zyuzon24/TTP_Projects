html
  head
    link(rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous")
    style 
      include style.css
  
  body
    div(class="theForm")
      h1  Weather App 
        form(action="/myform", method="POST", id="form")
          p(class="prompt") Enter a city to get its current weather:
            br
            input(value="", type="text", name="city")
          p
            button(class="btn btn-primary") Get Weather!
    div
      p(class="currentWeather") It is currently #{temp}° in #{city}
        
      p(class="currentWeather") Current weather conditions: #{description} 
        img(src= icon)
